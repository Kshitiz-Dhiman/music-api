version: 1
services:
  - type: web
    name: music-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FF_PATH
        value: /usr/bin/ffmpeg
      - key: YT_DLP_PATH
        value: /usr/bin/yt-dlp
